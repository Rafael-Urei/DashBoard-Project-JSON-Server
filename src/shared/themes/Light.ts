import { createTheme } from "@mui/material";

export const mainTheme = createTheme({
  palette: {
    primary: {
      main: "#00ABBD",
      light: "#A1C7E0",
      dark: "#026E81",
      contrastText: "#ffffff",
    },
  },
});
